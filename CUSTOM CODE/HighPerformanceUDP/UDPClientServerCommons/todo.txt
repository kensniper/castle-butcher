--najnowsze do zrobienia !!!!!!!!
--rozwiązać ostatecznie kwestie ACK (chociażby tymczasowo!!!) -- pominięte (czy słusznie?!)
--dopisać osbługę leaveGame i GameInfo  -- zrobione

 -- zrobić zakładanieGry, wyświetlanie aktualnych gier -- zrobione 
 
 DO ZROBIENIA 
wreszcie obsługa gracza-serwera
głupie eventy dla Kowala !!!!!
---------------------------------------------------------------------------------------
rozwiązać ostatecznie kwestie ACK (chociażby tymczasowo!!!)
dopisać osbługę leaveGame i GameInfo

zrobić zakładanieGry, wyświetlanie aktualnych gier
wreszcie obsługa gracza-serwera
głupie eventy dla Kowala !!!!!

---------------------------------------------------------------------------------------


nie potrzbna flaga biegnie,idzie,stoji, bo wszystko mozna przekazac predkoscia jako wektorem kierunku --ok



rozszerzyc klase JoinPacket   -- done
stworzyc klase GameInfoPacket  --done
stworzyc klase leaveGamePacket --done

bez ack
zrobic interfejs dla gracza-serwera z metoda ktora podaje ilosc zycia kazdego z graczy
lista zawierajace klase GameEvent (czas,rodzaj) (czas?)  --done

u klienta lista eventow - kazdy z czasem
grafika je zdjejmuje, sieciowosc wklada




-------------------------------
ponizej do przemyslenia

poprawic wysylanie ACK, teraz jest bez sensu bo marnuje sie tylko miejsce w pakiecie !!!

zaragowac na dane warstwy graficznej ktora podaje istotne dane,
tj. jesli warstwa podaje dane wymagajaca ack to sa one wysylane od razu jesli do nastepnego wyslania przez petle glowna zostalo >=50 ms 
jesli w tym czas nastapi kolejne zadania wyslania danych to zostane wyslane wiecej pakietow w odstepie 10 ms (jeden z nich moze zostac polaczony
z pakietem glownym jesli do jego wyslania zostalo mniej niz 50 ms)

serwer wysyla dane co 100 ms, jesli ma liste ack do wyslania to wysyla je jesli zostalo >50ms do petli glownej,

do dopracowania pozostaje ponowne wysylanie wiadomosci ktore ciagle czekaja na ACK